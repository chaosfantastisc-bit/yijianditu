# 天地图打不开：诊断与修复说明

## 结论
同一 Key 在 geo-downloader 中可以打开，而旧版一键地图打不开，原因是请求端不同：

- geo-downloader 的天地图预览由浏览器直接请求天地图瓦片；
- 旧版一键地图先请求本地 Python `/api/tile`，再由服务端请求天地图；
- 实测旧版服务端请求返回 HTTP 403，天地图提示该 Key 为服务器端权限类型，不允许按当前方式使用；
- 同一瓦片使用内置公共 Key 请求返回 HTTP 200，说明 URL 格式正常；不同 Referer A/B 测试未改变结果。

## 已完成修复
- 天地图预览改为浏览器直连，兼容 geo-downloader 使用的浏览器类型 Key。
- **修复子域（关键遗漏）**：`L.tileLayer` 未显式传入 `subdomains`，Leaflet 默认用 `a/b/c` 字母子域，
  天地图地址 `t{s}.tianditu.gov.cn` 会变成 `ta/tb/tc`，而天地图只提供 `t0~t7` 数字子域
  （实测 `ta.tianditu.gov.cn` 无法解析、`t0` 可连接）。已改为从配置下发数字子域 `0~7` 并传给 Leaflet，
  与 geo-downloader 的 `subdomains: ["0".."7"]` 完全一致。
- 输入框为空时，预览直接使用内置公共 Key，不会使用本机历史失效 Key。
- 输入有效 Key 后，输入停止 500ms 自动刷新天地图图层。
- 估算/下载接口在天地图 Key 留空时显式传空字符串，服务端恢复内置公共 Key，并清理失效的本机记忆 Key。
- ArcGIS 仍保持本地代理方式，不受影响。

## 验证
- JavaScript 语法检查通过。
- Python 语法检查通过。
- 内置公共 Key 请求天地图影像瓦片：HTTP 200，正常 JPG。

## 注意
天地图影像下载仍由本地 Python 服务端执行。如果使用的是浏览器类型 Key，预览可以打开，但下载阶段可能仍需要申请天地图“服务器端”权限 Key；这是天地图权限策略限制，不是程序绕过。 
